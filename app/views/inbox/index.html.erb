<ul style="list-style:none">
  <% @messages.each do |id, message| %>
    <li><%= message.first.subject %></li>
    <li><%= time_ago_in_words(message.first.created_at) %></li>
    <li><%= link_to("Message", message_path(message.first.id))%></li>
  <% end %>
</ul>